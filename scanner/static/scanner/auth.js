'use strict';(()=>{const csrf=()=>document.cookie.split('; ').find(x=>x.startsWith('csrftoken='))?.split('=').slice(1).join('=')||'';async function send(url,body){const r=await fetch(url,{method:'POST',body,headers:{'X-CSRFToken':csrf()}});const d=await r.json();if(!r.ok)throw new Error(d.error||'Please try again.');location.href='/';}document.querySelector('.pin-form')?.addEventListener('submit',async e=>{e.preventDefault();const out=document.getElementById('auth-error');out.hidden=true;try{await send(e.target.action,new FormData(e.target));}catch(x){out.textContent=x.message;out.hidden=false;}});document.getElementById('signout-form')?.addEventListener('submit',async e=>{e.preventDefault();await send('/auth/logout/',new FormData());});})();
